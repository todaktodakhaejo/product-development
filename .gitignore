# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock
# flutter 툴 크래시 덤프(분석 서버 크래시 등)
flutter_*.log

# Android 서명 키(절대 커밋 금지 — CI는 GitHub Secrets로 주입)
*.jks
*.keystore
*.p12
android/key.properties
android/app/upload-keystore.p12

# IDE
.idea/
.vscode/
*.iml

# Generated native folders (run `flutter create .` to regenerate)
# (keep these tracked if you customize them)
/web/

# macOS
.DS_Store
# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# 하네스
.claude/
CLAUDE.md
# 에이전트 작업 산출물(중간 산출물 — 로컬 보존, PR 제외)
_workspace/
_workspace_prev/
