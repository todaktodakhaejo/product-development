/// 경험 루프의 단계 (docs/PRODUCT_SPEC.md 1.2 / 3장).
///
/// Soothe(진정) → Pour(분출) → ReleaseSelect(의식 선택) → ReleaseRitual(의식 수행) → Closing(마무리).
enum RitualPhase {
  soothe,
  pour,
  releaseSelect,
  releaseRitual,
  closing,
}
